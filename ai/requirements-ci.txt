# ============================================================================
# CloudAI Fusion - CI/Test Dependencies (lightweight)
# ============================================================================
# Excludes heavy ML libraries (torch, prophet, transformers, stable-baselines3)
# that are optional and gracefully handled via try/except ImportError.
# ============================================================================

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# --- Data Processing (core only) ---
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
scikit-learn>=1.4.0

# --- LLM Integration ---
openai>=1.14.0
httpx>=0.27.0

# --- HTTP & Async ---
aiohttp>=3.9.0
redis>=5.0.0

# --- 可观测性 ---
prometheus-client>=0.20.0
opentelemetry-api>=1.23.0,<3.0.0
opentelemetry-sdk>=1.23.0,<3.0.0
opentelemetry-exporter-otlp>=1.23.0,<3.0.0

# --- Utilities ---
pyyaml>=6.0
python-dotenv>=1.0.0
structlog>=24.1.0
tenacity>=8.2.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
