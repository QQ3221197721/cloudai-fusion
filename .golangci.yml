# ============================================================================
# golangci-lint v2 configuration for CloudAI Fusion
# Run: golangci-lint run ./...
# Migrated from v1 → v2 format (required for Go 1.25+ support)
# ============================================================================

version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

output:
  formats:
    - format: colored-line-number
  sort-results: true

linters:
  enable:
    # Default linters
    - errcheck        # Check for unchecked errors
    - gosimple        # Simplify code
    - govet           # Vet examines Go source code
    - ineffassign     # Detect ineffectual assignments
    - staticcheck     # Static analysis checks
    - unused          # Check for unused code

    # Additional linters
    - bodyclose       # Check HTTP response body is closed
    - copyloopvar     # Detect loop variable capture issues
    - dupl            # Code clone detection
    - durationcheck   # Check for two durations multiplied together
    - errname         # Check error naming conventions
    - errorlint       # Wrapping errors with fmt.Errorf
    - exhaustive      # Check exhaustive switch statements
    - gocognit        # Cognitive complexity
    - goconst         # Find repeated strings that could be constants
    - gocritic        # Opinionated Go source code linter
    - gocyclo         # Cyclomatic complexity
    - gosec           # Security checks
    - misspell        # Find misspelled English words
    - nakedret        # Find naked returns in long functions
    - nilerr          # Find code that returns nil even if error is not nil
    - noctx           # Find HTTP requests without context.Context
    - prealloc        # Find slice declarations that could be preallocated
    - predeclared     # Find shadowed predeclared identifiers
    - revive          # Extensible linter (successor of golint)
    - unconvert       # Remove unnecessary type conversions
    - unparam         # Find unused function parameters
    - wastedassign    # Find wasted assignment statements

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: false

    gocyclo:
      min-complexity: 25

    gocognit:
      min-complexity: 40

    dupl:
      threshold: 200

    goconst:
      min-len: 3
      min-occurrences: 4

    gocritic:
      enabled-tags:
        - diagnostic
        - performance
      disabled-checks:
        - ifElseChain
        - hugeParam

    gosec:
      excludes:
        # G404: Insecure random number source (math/rand) — acceptable for non-security
        # purposes like jitter, load balancing, and test data generation.
        # Security-critical randomness uses crypto/rand (auth tokens, secrets, etc.)
        - G404  # Insecure random number source (math/rand)

    nakedret:
      max-func-lines: 30

    revive:
      rules:
        - name: blank-imports
        - name: context-as-argument
        - name: context-keys-type
        - name: dot-imports
        - name: error-return
        - name: error-strings
        - name: exported
          disabled: true  # Too noisy for internal packages
        - name: increment-decrement
        - name: indent-error-flow
        - name: range
        - name: receiver-naming
        - name: time-naming
        - name: unexported-return
        - name: var-declaration
        - name: var-naming

    exhaustive:
      default-signifies-exhaustive: true

    misspell:
      locale: US

  exclusions:
    rules:
      # Test files can have longer functions and more complexity
      - path: _test\.go
        linters:
          - dupl
          - gocyclo
          - gocognit
          - errcheck
          - gosec
          - unparam
          - goconst

      # Generated / mock files
      - path: mock_
        linters:
          - dupl
          - unused
          - unparam

      # Main entry points
      - path: cmd/
        linters:
          - gocyclo

      # Allow fmt.Println in examples
      - path: examples/
        linters:
          - gosimple

    # v2: issue limits moved here
    max-issues-per-linter: 50
    max-same-issues: 5

# Formatters (v2: separated from linters)
formatters:
  enable:
    - gofmt
    - goimports

severity:
  default: warning
  rules:
    - linters:
        - govet
        - errcheck
        - staticcheck
      severity: error
